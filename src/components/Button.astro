---
const {
    label = "Button",
    onClick,
    disabled = false,
    type = "button",
    className = "",
    ...props
} = Astro.props;
---

<button
    class={`px-4 py-2 rounded-md font-medium transition-colors duration-200 ${className} ${disabled ? "opacity-50 cursor-not-allowed" : "hover:bg-primary-600 bg-primary-500 text-white"}`}
    onclick={onClick}
    disabled={disabled}
    type={type}
    {...props}
>
    {label}
</button>
